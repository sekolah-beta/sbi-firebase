name: sbi_flutter_firebase
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  dartz: ^0.10.1
  firebase_core: ^3.1.1
  firebase_database: ^11.0.2
  flutter:
    sdk: flutter
  overlay_loading_progress: ^1.0.1
  uuid: ^4.4.0
  firebase_auth: ^5.1.2
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
